{
  "name": "bis-tracker-addon",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Test harness for the BisWatcher WoW addon.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fengari": "^0.1.4",
    "vitest": "^3.0.5"
  }
}
